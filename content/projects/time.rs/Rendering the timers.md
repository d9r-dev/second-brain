* List of timers
* are they sorted by date already?
* if yes, take first and safe the day
* render a header of the day
* for all items if the day is the same as saved day render the items
* if not render a new day header
* render items and so on
* highlight the last item
* the actual running timer should be rendered differently and gets updated every second and has an icon